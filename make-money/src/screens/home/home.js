import { Image, Text, View } from "react-native"
import icons from "../../constants/icons.js";
const Home = () => {
    return (<View>
        <Image source={icons.db} />
    </View>
    );
} 

export default Home;